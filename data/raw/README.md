# Raw data

Place new UTF-8 `.txt`, `.md`, `.json`, and JSON conversation-export files here. The manual agent reads this directory recursively, skips this README, and treats every changed file as a new immutable source version. See [`../../agent/README.md`](../../agent/README.md) for supported extraction syntax.
