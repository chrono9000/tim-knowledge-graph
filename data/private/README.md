# Private master graph

`master-graph.json` is created locally after an approval and is ignored by Git. It contains approved private and public knowledge. Never place this directory in a Pages artifact.
