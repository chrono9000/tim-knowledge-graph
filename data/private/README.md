# Private master graph

`master-graph.json` is created locally after an approval and is ignored by Git. It contains approved private and public knowledge. `access-approval.json` records Tim's selected private-viewing design locally. Never place this directory in a Pages artifact. Use `python -m agent view-private` to view the master without copying it into the public site.
