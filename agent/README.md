# Agent workspace

Placeholder for future agent instructions and orchestration code. No agent, ingestion process, bot, or scheduled job is implemented yet.

Future work should treat [`../schemas/graph.schema.json`](../schemas/graph.schema.json) as the write contract and should stage source material in `data/raw/` and normalized records in `data/processed/` before proposing changes to `data/graph.json`.
